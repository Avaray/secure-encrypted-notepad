name: Release

on:
  push:
    branches: ["main"]
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 1.2.3)'
        required: true
        type: string

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  check-version:
    name: Check Version & Tag
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
      created: ${{ steps.check_tag.outputs.created }}
    steps:
      - uses: actions/checkout@v6
      
      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Get version from Cargo.toml
        id: get_version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
            echo "Using manually provided version: $VERSION"
          else
            VERSION=$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')
            echo "Detected version from Cargo.toml: $VERSION"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if tag exists
        id: check_tag
        run: |
          # Check if tag v{version} already exists in remote
          if git ls-remote --exit-code --tags origin "v${{ steps.get_version.outputs.version }}" >/dev/null 2>&1; then
            echo "Tag v${{ steps.get_version.outputs.version }} already exists. Skipping release."
            echo "created=false" >> $GITHUB_OUTPUT
          else
            echo "Tag v${{ steps.get_version.outputs.version }} does not exist. Proceeding with release."
            echo "created=true" >> $GITHUB_OUTPUT
          fi

  build-and-release:
    name: Build & Release (Windows)
    needs: check-version
    if: needs.check-version.outputs.created == 'true'
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Run Tests
        run: cargo test --release --verbose

      - name: Build Release Binary
        run: cargo build --release --verbose

      - name: Rename Binary
        run: |
          mv target/release/sen.exe target/release/sen-v${{ needs.check-version.outputs.version }}.exe
        shell: bash

      - name: Generate Checksum
        run: |
          cd target/release
          sha256sum sen-v${{ needs.check-version.outputs.version }}.exe > sen-v${{ needs.check-version.outputs.version }}.exe.sha256
        shell: bash

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.check-version.outputs.version }}
          name: v${{ needs.check-version.outputs.version }}
          files: |
            target/release/sen-v${{ needs.check-version.outputs.version }}.exe
            target/release/sen-v${{ needs.check-version.outputs.version }}.exe.sha256
          generate_release_notes: true
          draft: false
          prerelease: false
          token: ${{ secrets.RELEASE_PAT }}
